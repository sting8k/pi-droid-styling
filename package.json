{
  "name": "@anthropic/pi-droid-styling",
  "version": "1.0.0",
  "description": "Custom UI styling for pi — boxed editor, tool badges, message prefixes, chat virtualization. Works with any theme.",
  "main": "index.ts",
  "keywords": [
    "pi-package"
  ],
  "scripts": {
    "dev:link": "npm install",
    "dev:clean": "rm -rf node_modules"
  },
  "dependencies": {
    "pi-ctx-kit": "file:../pi-ctx-kit"
  },
  "pi": {
    "extensions": [
      "./index.ts"
    ]
  },
  "peerDependencies": {
    "@mariozechner/pi-coding-agent": "^0.72.1",
    "@mariozechner/pi-tui": "^0.72.1"
  },
  "devDependencies": {
    "@mariozechner/pi-coding-agent": "^0.72.1",
    "@mariozechner/pi-tui": "^0.72.1"
  }
}
